name: CI
on:
  workflow_dispatch:
  pull_request:
    branches:
      - master
jobs:
  serverless:
    name: CI
    uses: Life-Control/workflows/.github/workflows/laravel-serverless-CI.yml@master
    with:
      php_version: '8.1'